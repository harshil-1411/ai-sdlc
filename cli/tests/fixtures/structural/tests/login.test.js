describe("login", () => {
  it("REQ-FIX-12: rejects a bad password", () => {
    expect(true).toBe(true);
  });

  // covers REQ-FIX-17
  it("accepts a good password", () => {
    expect(true).toBe(true);
  });
});

// it("REQ-FIX-19: a commented-out test is not a test", () => {});
