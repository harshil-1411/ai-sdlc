---
max_turns: 20
timeout_seconds: 400
allowed_tools: [Read, Glob, Grep, Skill, Agent]
tags: [behavior, agent, code-reviewer, scaffold]
---

PERM-12 is ready to push. It's Tier 3, so it needs the full review before the
push — the branch diff is saved at review/PERM-12.diff (there's no git remote
in this sandbox). Please run it.
