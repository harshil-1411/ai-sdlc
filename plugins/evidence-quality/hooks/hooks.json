{
  "description": "Advisory test-coverage check at session start. The commit gate (tracker key, Agent-Session trailer, staged-secret scan) moved into the evidence-sdlc gate engine in v2.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": ["-c", "command -v python3 >/dev/null 2>&1 && exec python3 \"$0\" || cat >/dev/null", "${CLAUDE_PLUGIN_ROOT}/scripts/check-test-plan-rows.py"],
            "timeout": 20
          }
        ]
      }
    ]
  }
}
