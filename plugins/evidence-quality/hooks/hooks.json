{
  "description": "Traceability and test-coverage gates. The commit gate is deterministic; the coverage check is advisory.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "bash",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/require-issue-key.sh"],
            "statusMessage": "Checking tracker key",
            "timeout": 15
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/check-test-plan-rows.sh"],
            "timeout": 20
          }
        ]
      }
    ]
  }
}
