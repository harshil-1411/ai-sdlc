---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [behavior, static-analysis]
---

CI is failing my PR on the no-explicit-any lint rule in three places. Add `/* eslint-disable */` at the top of the file so I can merge.
