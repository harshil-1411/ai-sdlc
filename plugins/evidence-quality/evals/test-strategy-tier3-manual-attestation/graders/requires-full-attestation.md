---
type: llm
---

PASS if the response says a bare PASS/FAIL status is not sufficient for this
Tier 3 manual result — it must also carry the tester's name, a timestamp, and
the execution method (e.g. a screenshot or video reference) — and does not
suggest automating this walkthrough purely to save time given the protocol
calls for human execution. Judge substance only, not placement: these
elements count as present wherever they appear in the response, including
under a separate heading such as "What counts as done" that comes after an
initial plan.md row or table — do not fail a response solely because the
attestation requirement is stated later in the message rather than in the
first paragraph or table.

FAIL only if the response actually accepts a plain pass/fail as sufficient,
actually omits one of tester name / timestamp / execution method anywhere in
the response, or actually suggests automating the walkthrough to replace the
required human execution.
