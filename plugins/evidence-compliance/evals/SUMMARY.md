# evidence-compliance eval summary

No with-vs-without run was recorded for this plugin in the v2 pass.

Not run in the v2 pass: the maintainer's verification budget ($80) was spent on the other four plugins after a mid-run regression (the plugin.json `dependencies` field) forced a second pass. Run `claude plugin eval . --scaffold --runs 3` in this directory, or the CI `evals` job, to produce it. Δ (with vs without) is therefore unmeasured here.
