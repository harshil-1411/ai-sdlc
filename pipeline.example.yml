# Superseded in v2 (PILOT-53). The v1 file here was illustrative pseudo-YAML.
# Real, runnable reference pipelines now live in pipelines/:
#   pipelines/github-actions/evidence-chain.yml   -- commit + evidence stages for an adopting repo
#   pipelines/github-actions/agent-in-ci.yml      -- CI-hosted agent that can only open PRs
#   pipelines/gitlab/evidence-chain.gitlab-ci.yml -- the same stages for GitLab
# This repository's own CI is .github/workflows/ci.yml.
{}
